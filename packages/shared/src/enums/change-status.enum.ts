export enum ChangeStatus {
    SUGGESTED = 'suggested',
    APPLIED = 'applied',
    REJECTED = 'rejected',
}

export enum ChangeType {
    INSERT = 'insert',
    REPLACE = 'replace',
    DELETE = 'delete',
}
