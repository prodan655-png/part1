# 🛠️ Налаштування середовища (macOS)

Для запуску проєкту потрібно встановити **Node.js** та **pnpm**.

### Варіант 1: Через Homebrew (Рекомендовано)

Якщо у вас є Homebrew:

```bash
# 1. Встановити Node.js (версія 18 або новіша)
brew install node@18

# 2. Додати node в PATH (виконайте команди, які підкаже brew після встановлення, зазвичай це:)
echo 'export PATH="/opt/homebrew/opt/node@18/bin:$PATH"' >> ~/.zshrc
source ~/.zshrc

# 3. Встановити pnpm
brew install pnpm

# 4. Перевірити
node -v
pnpm -v
```

### Варіант 2: Через NVM (Node Version Manager)

```bash
# 1. Встановити nvm
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.5/install.sh | bash

# 2. Перезапустити термінал або виконати:
source ~/.zshrc

# 3. Встановити Node.js
nvm install 18
nvm use 18

# 4. Встановити pnpm
npm install -g pnpm
```

---

### Після встановлення:

Поверніться в папку `apps/web` і спробуйте знову:

```bash
cd apps/web
pnpm install
pnpm dev
```
